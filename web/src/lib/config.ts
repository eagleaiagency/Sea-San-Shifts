export const MANAGER_EMAIL = "seasanorl@gmail.com";

export const MANAGER_PIN = "5050";

// Folga precisa ser com 14 dias de antecedência
export const TIMEOFF_MIN_DAYS = 14;

// Roles padrão (gerente pode escrever qualquer texto também)
export const DEFAULT_ROLES = ["server", "bartender", "kitchen", "dishwasher"] as const;
